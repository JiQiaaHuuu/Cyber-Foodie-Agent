# 工程实践报告说明

已生成可用 Word 打开的报告文件：

`CyberFoodieAgent/reports/2026-2027-0 XXX老师-XXX学生(学号)-工程实践报告-CyberFoodieAgent.doc`

该文件本质是带排版 CSS 的 HTML 文档，扩展名为 `.doc`，Word 可以打开并继续编辑。你只需要把封面的 `XXX 老师`、`XXX`、`XXX` 替换为自己的老师姓名、学生姓名和学号。

如果需要严格 `.docx` 模板版，可以在 Word 中打开该文件后另存为 `.docx`。
