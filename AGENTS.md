# AI 协作规则

## 架构约束
- 后端业务逻辑放在 `src/cyberfoodie/`，入口文件只负责启动服务。
- 前端静态资源放在 `src/static/`。
- 真实菜单数据放在 `src/data/real_menu.json`。
- 不允许提交真实 API Key、日志、缓存或本地 IDE 配置。

## 代码规则
- DeepSeek 调用必须保留本地兜底逻辑，保证课堂演示不中断。
- Agent 开赛前必须锁定固定主推菜，三轮辩论中不得更换推荐菜。
- 裁判只能在两个 Agent 的固定主推菜中选择最终推荐。
